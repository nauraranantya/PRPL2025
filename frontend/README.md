Frontend for Village Events

- Run `npm install` then `npm start`.

- This frontend is configured to call the backend at http://localhost:4000/api by default.
- To change, set REACT_APP_API_BASE in a .env file at project root.
